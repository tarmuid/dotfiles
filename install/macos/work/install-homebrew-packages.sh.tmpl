#!/usr/bin/env zsh

set -euo pipefail

if [[ "{{ .chezmoi.os }}" != "darwin" ]]; then
  exit 0
fi

if [[ "{{ .flags.is_work_owned }}" != "true" ]]; then
  exit 0
fi

{{ include "../install/macos/work/install-homebrew-packages.sh.age" | decrypt }}
